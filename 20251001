-- 1. Set 테이블의 모든 데이터를 깨끗하게 비웁니다.
TRUNCATE TABLE excluded_pkt_seq_set;

-- 2. 현재 TEST_LOG에 남아있는 (삭제되지 않은) 데이터로 Set 테이블을 다시 채웁니다.
INSERT INTO excluded_pkt_seq_set SELECT DISTINCT PKT_SEQ FROM TEST_LOG;
